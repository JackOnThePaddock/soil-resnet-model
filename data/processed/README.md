# Processed Data

This directory contains processed datasets generated by the data pipeline.

## How to Generate

```bash
python scripts/run_data_extraction.py --output data/processed/
python scripts/run_gee_sampling.py --csv data/processed/soil_sites.csv --output data/processed/features.csv
```

Processed data is not tracked in git. See `data/raw/` for source data.
